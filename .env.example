# Docker Compose Environment Variables

# Web Application - API URL
# For development, the web app will connect to the API at this URL
# When running in Docker, use the host URL so browsers can access it
VITE_API_BASE_URL=http://localhost:5202/

# Google OAuth Configuration
# Get your client ID from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OpenTelemetry Configuration (optional)
# OTEL_URL=http://your-otel-collector:4317

# Database Configuration (already set in docker-compose.yml)
# These are the default values used by the database service
# POSTGRES_USER=consilium-admin
# POSTGRES_PASSWORD=mypassword
# POSTGRES_DB=consiliumdb
