# Production Environment Variables
# These values are injected at build time by GitHub Actions from GitHub Secrets
# 
# Expected format for VITE_API_BASE_URL:
# https://consilium-api-<hash>.azurewebsites.net/
# (must include trailing slash and no path prefix)

VITE_API_BASE_URL=${VITE_API_BASE_URL}
VITE_GOOGLE_CLIENT_ID=${VITE_GOOGLE_CLIENT_ID}
